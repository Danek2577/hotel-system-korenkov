# 🏨 Hotel Management System

Полнофункциональная система управления отелем с современным стеком технологий.

## 🚀 Стек технологий

### Backend
- **Node.js** + **Express.js** - серверный фреймворк
- **MySQL** + **Sequelize ORM** - база данных
- **JWT** + **BCrypt** - аутентификация
- **Joi** - валидация данных

### Frontend
- **Next.js** (Pages Router) + **TypeScript**
- **NextUI** + **Tailwind CSS** - UI компоненты
- **MobX** - управление состоянием
- **SWR** - кэширование и запросы
- **Axios** - HTTP клиент

## 📁 Структура проекта

```
hotel-system/
├── server/                 # Backend
│   ├── controllers/        # Контроллеры
│   ├── service/           # Бизнес-логика (Singleton)
│   ├── models/            # Sequelize модели
│   ├── routers/           # Express роутеры
│   ├── middleware/        # Middleware (auth, endHandler)
│   └── utils/             # Утилиты и валидация
│
└── client/                # Frontend
    ├── pages/             # Next.js страницы
    │   ├── lk/           # Личный кабинет
    │   └── login/        # Авторизация
    └── src/
        ├── API/          # Axios запросы
        ├── components/   # React компоненты
        │   └── module/   # Модули (room, booking)
        ├── store/        # MobX сторы
        └── utils/        # Утилиты
```

## 🗄️ База данных

### Таблицы
- **users** - пользователи системы (ADMIN, MANAGER)
- **rooms** - номера отеля (STANDARD, LUXURY, SUITE)
- **bookings** - бронирования

### Особенности
- Все даты хранятся как UNIX timestamp
- Soft delete через поле `date_delete`
- Проверка коллизий бронирований

## 🔧 Установка и запуск

### 1. Клонирование и настройка

```bash
cd hotel-system

# Backend
cd server
cp .env.example .env
# Отредактируйте .env с вашими настройками БД
npm install

# Frontend
cd ../client
cp .env.local.example .env.local
npm install
```

### 2. Настройка базы данных

Создайте базу данных MySQL:
```sql
CREATE DATABASE hotel_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 3. Запуск

```bash
# Backend (в папке server)
npm run dev

# Frontend (в папке client)
npm run dev
```

Откройте http://localhost:3000

## 📡 API Endpoints

### Auth
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/check` - Проверка авторизации

### Rooms
- `GET /api/rooms` - Публичный список номеров
- `GET /api/rooms/:id` - Публичный номер
- `GET /api/rooms/adm/list` - Список номеров (Admin)
- `POST /api/rooms/adm` - Создание номера
- `PUT /api/rooms/adm/:id` - Обновление номера
- `DELETE /api/rooms/adm/:id` - Удаление номера

### Bookings
- `GET /api/bookings/availability` - Проверка доступности
- `GET /api/bookings/adm` - Список бронирований
- `POST /api/bookings/adm` - Создание бронирования
- `PUT /api/bookings/adm/:id` - Обновление бронирования
- `PUT /api/bookings/adm/:id/cancel` - Отмена бронирования
- `DELETE /api/bookings/adm/:id` - Удаление бронирования

## 🔐 Авторизация

- Роли: **ADMIN**, **MANAGER**
- JWT токен хранится в localStorage
- Автоматическое обновление при 401 ошибке

## 🎨 UI Features

- ✅ Таблицы с пагинацией и поиском
- ✅ Модальные формы создания/редактирования
- ✅ Проверка доступности номера в реальном времени
- ✅ Русскоязычный интерфейс
- ✅ Toast уведомления
- ✅ Адаптивный дизайн

## 📝 Валидация

Все сообщения об ошибках на русском языке:
- "Поле 'Электронная почта' обязательно для заполнения"
- "Цена должна быть положительным числом"
- "На выбранные даты номер уже занят"

## 🛠 Разработка

### Архитектурные принципы
- **3-Layer Architecture**: Controller → Service → Model
- **Singleton Services**: `module.exports = new Service()`
- **Транзакции** для операций с несколькими записями
- **SWR Hooks** вместо useEffect для data fetching

### Нейминг
- Backend: `roomService`, `bookingController`
- Frontend: `useRoomsAdmGet`, `fetchBookingAdmCreate`
- Методы: `admCreate`, `admUpdate`, `admGet`, `admGetOne`

## 📄 Лицензия

MIT
